#include "main.h"

/**
 * mul - multiplies two intagers.
 * @a: The first intager to be multiplied.
 * @b: The second intager to be multiplied.
 *
 * Return: The result of the multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
